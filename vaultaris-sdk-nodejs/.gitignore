# napi-rs build artifacts — generated by `npm run build`.
index.js
index.d.ts
*.node

# npm scratch — `package-lock.json` is intentionally not committed; the
# Node SDK is built from `Cargo.toml` plus the platform-specific
# `napi build` outputs, and the lockfile changes per CI runner.
package-lock.json
node_modules/
